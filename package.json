{
  "name": "tekkzy-fit-api",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "deploy": "serverless deploy --stage dev --region us-east-2",
    "deploy:beta": "serverless deploy --stage dev --region us-east-2",
    "deploy:prod": "serverless deploy --stage prod --region us-east-2",
    "offline": "serverless offline"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.899.0",
    "@aws-sdk/lib-dynamodb": "^3.899.0",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {}
}
